#
# YAM MANIFEST file for front!st!geo
#
# History:
#       16-Mar-2010 (troal01)
#           Added
#       31-Aug-2010 (troal01)
#           Added calls to the IIGeoLibs rule

IISUBSYS front st geo ;

IIDFILES geo : srs.dat ;

if $(NT)
{
    IIGeoLibs $(SHGEOSCLIB) $(SHPROJLIB) ;
}
else
{
    IIGeoLibs $(SHGEOSLIB) $(SHGEOSCLIB) $(SHPROJLIB) ;
}
